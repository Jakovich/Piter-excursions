- var menu = { 'Прислать историю': '/history-add.html', 'Регистрация': '#', 'Экскурсии дешево': '/excursions-cheap.html',  'Экскурсии дорого': '/excursions-pricy.html'};
header.main-header
  nav.main-nav
      .wrapper
          .main-nav__content
              .main-nav__logo
                  a(href="index.html"): img(src="img/logo.svg" width="68"   height="53")
              .main-nav__items-wrp
                  ul.main-nav__items
                      each val, key in menu
                          if pageTitle === key
                              li(class=["main-nav__item", "main-nav__item--active"])
                                  a(href='#', title=key)= key
                          else
                              li(class=["main-nav__item"])
                                  a(href=val, title=key)= key
                  button.main-nav__close
                      img(src="img/icon-close.svg" width="30")
              button.main-nav__toggle
                  img(src="img/icon-menu.svg" width="30")
  .wrapper
    h1.main-title Экскурсии для людей
  
